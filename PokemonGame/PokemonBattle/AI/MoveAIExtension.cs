using PokemonGame.PokemonBattle.Entities;
using PokemonGame.PokemonBattle.Extensions;
using PokemonGame.PokemonBattle.Enums;

namespace PokemonGame.PokemonBattle.AI;

public static class MoveAiExtension
{
    /*
    public static bool CanLowerSpeed(this Move move) => false; // TODO

    public static bool DoesLowerSpeed(this Move move) => false; // TODO

    public static bool AffectsPokemon(this Move move, Pokemon user, Pokemon target)
    {
        // TODO this doesnt account for abilities like normalize => should use CheckMoveType method instead.
        return BattleCalculationExtension.CalculateEffectivityMod(move, target.Types) > 0;
    }
    */
}